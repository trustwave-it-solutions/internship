@model IEnumerable<expense_tracher.Models.TblTransaction>

@{
    ViewData["Title"] = "Index";
}

<h1>Income</h1>
<div class="row justify-content-end mb-4">
    <div class="col-sm-6"><a href="#" class="primary-btn float-end">Add Income</a></div>
</div>
<div class="row">
    <div class="col-md-8""></div>
    <div class="col-sm-4">
        <input class="form-control float-end" placeholder="Search" />
    </div>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaymentMode)
            </th>
            <th>
                Action
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>Anu</td>
            <td> 2000</td>
            <td>Rent</td>
            <td>UPI</td>
            <td><i class="bi bi-trash3-fill me-3"></i><i class="bi bi-pencil me-3"></i><i class="bi bi-layout-text-window-reverse me-3"></i></td>
        </tr>
        <tr>
            <td>Linu</td>
            <td> 1000</td>
            <td>Groceries</td>
            <td>UPI</td>
            <td><i class="bi bi-trash3-fill me-3"></i><i class="bi bi-pencil me-3"></i><i class="bi bi-layout-text-window-reverse me-3"></i></td>
        </tr>
        <tr>
            <td>Snigdha</td>
            <td> 3000</td>
            <td>Rent</td>
            <td>UPI</td>
            <td><i class="bi bi-trash3-fill me-3"></i><i class="bi bi-pencil me-3"></i><i class="bi bi-layout-text-window-reverse me-3"></i></td>
        </tr>
        <tr>
            <td>Hima</td>
            <td> 5000</td>
            <td>Rent</td>
            <td>UPI</td>
            <td><i class="bi bi-trash3-fill me-3"></i><i class="bi bi-pencil me-3"></i><i class="bi bi-layout-text-window-reverse me-3"></i></td>
        </tr>
@* @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Note)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModifiedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDeleted)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaymentMode.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaymentType.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
} *@
    </tbody>
</table>
